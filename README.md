# DesignPatterns_HW

This project simulates a restaurant ordering system using the following design patterns:
- Factory Pattern for food creation
- Strategy Pattern for payment processing

Flow:
User selects items -> Factory creates them -> Order calculates total -> Strategy handles payment
